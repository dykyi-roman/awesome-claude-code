---
type: tool_used
tool: "Task"
min: 1
---

The three-layer contract is command → agent → skill. If the Task tool is never called, the
command inlined the analysis instead of delegating, and the agent layer is dead weight.
